/*
  Buatlah sebuah function yang akan mengembalikan nilai (return) lalu panggilah function tersebut!
*/

/*
  Tanpa menjalankan kodingannya!
  Apa output yang akan muncul di terminal apabila function dibawah ini dijalankan!

  function panggil3(pararam1, pararam2, pararam3) {
    return pararam2 + pararam3 + pararam1
  }
  console.log(panggil3(30, 10, "abc"))
*/

/*
  Tanpa menjalankan kodingannya!
  Apakah pemanggilan function dibawah ini benar?
  Apa output tiap pemanggilan function dibawah ini?

  let uang = 50000
  function bayar(money) {
    let kembalian = uang - 30000
    return kembalian
  }
  console.log(bayar(uang)) // output1?
  console.log(bayar(100_000)) // output2?
  console.log(bayar(200_000)) // output3?
*/

/*
  Buatlah function yang menggunakan return yang tujuannya adalah untuk validasi input.
  Function hanya memiliki 1 parameter yaitu input.
  Validasi inputnya harus meliputi ini
  - jika input mempunyai panjang dibawah 6, maka outputnya adalah "input kurang panjang"
  - jika input bukan berupa string, maka outputnya adalah "input bukan string"
  - jika input undefined, maka outputnya adalah "input tidak terdifinisikan"
  - jika input tidak terdapat huruf besar maka outputnya adalah "input tidak terdapat huruf besar"
  - jika tidak masuk kondisi diatas, maka outputnya adalah "input sudah benar" 

  Sesuaikan argument yang dikirim dengan case agar kondisi diatas dapat terpenuhi semua
*/
